import React from "react";
import {
  Box,
  Button,
  FormControl,
  FormLabel,
  Select,
  SimpleGrid,
  Heading,
} from "@chakra-ui/react";

const DrillUnitMonthlyProgressReport = () => {
  return (
    <Box p={6} bg="white" maxW="800px" mx="auto" mt={8} borderRadius="md" boxShadow="md">
      <Heading size="md" mb={6} color="brown">
        Drill Unit Monthly Progress Report
      </Heading>

      <SimpleGrid columns={{ base: 1, md: 2 }} spacing={6}>
        <FormControl isRequired>
          <FormLabel>Field Season Year</FormLabel>
          <Select placeholder="--Please Select--" />
        </FormControl>

        <FormControl isRequired>
          <FormLabel>Unit No</FormLabel>
          <Select placeholder="--Please Select--" />
        </FormControl>

        <FormControl isRequired>
          <FormLabel>Month</FormLabel>
          <Select placeholder="--Please Select--" />
        </FormControl>

        <FormControl isRequired>
          <FormLabel>FSP Code</FormLabel>
          <Select placeholder="--Please Select--" />
        </FormControl>

        <FormControl isRequired>
          <FormLabel>Region / Others</FormLabel>
          <Select placeholder="--Please Select--" />
        </FormControl>
      </SimpleGrid>

      <Button
        mt={6}
        colorScheme="orange"
        bg="brown"
        color="white"
        _hover={{ bg: "orange.600" }}
      >
        Generate Report
      </Button>
    </Box>
  );
};

export default DrillUnitMonthlyProgressReport;
